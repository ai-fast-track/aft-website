spec:
  name: aft-website
  static_sites:
  - build_command: rm -r ./public; hugo --minify --destination ./public
    environment_slug: hugo
    git:
      branch: main
      repo_clone_url: https://github.com/ai-fast-track/aft-website.git
    name: aft-website